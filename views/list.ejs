<%- include("header") -%>
    <div class="box" id="heading">
        <h1>
            <%= listTitle %>
        </h1>
    </div>

    <div class="box">
        <form action="/delete" method="post">
        <% for(var i=0 ; i < newListItem.length ; i++) { %>
            <div class="item">
                    <button type="submit" name="delete" value="<%=newListItem[i]%>">-</button>
                    <p><%=newListItem[i]%></p>
                </div>
                <%} %>
            </form>
    </div>


    <div class="box">
        <form action="/" method="post">
            <input type="text" name="newItem" placeholder="New Item" autocomplete="off">
            <button type="sumbmi" name="list" value=<%= listTitle %>>+</button>
        </form>
    </div>

<%- include("footer") -%>


